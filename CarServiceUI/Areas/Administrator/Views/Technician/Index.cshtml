@model IEnumerable<CarServiceBL.Models.Technician>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

@if (ViewBag.IsDeleted > 0)
{
    <a asp-action="ViewAllDeleted" class="btn btn-success">ViewAllDeleted</a>
}

<br />
<br />

@if (Model.Any())
{
    <a asp-action="Create" class="btn btn-success">Create</a>
    <table class="table table-bordered">
        <tr>
            <td>@Html.DisplayNameFor(x=>x.TechnicianName)</td>
            <td>@Html.DisplayNameFor(x=>x.TechnicianDescription)</td>
        </tr>

        @foreach (var i in Model)
        {

            <tr>
                <td>@i.TechnicianName</td>
                <td>@i.TechnicianDescription</td>
                <td>
                    <img src="~/img/@i.ImagePath" width="50" class=" img-thumbnail  rounded-circle"/>
                </td>
                <td>
                    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@i.TechnicianId">Edit</a>
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@i.TechnicianId">Delete</a>
                    <a asp-action="SoftDelete" class="btn btn-danger" asp-route-id="@i.TechnicianId">SoftDelete</a>
                </td>
            </tr>
        }
    </table>
}
else
{
    <div>
        <p> No Tec Created Yet  if  You  want u Can Create A one From Here  <p />
        <a asp-action="Create" class="btn btn-success">Create</a>
    </div>
}